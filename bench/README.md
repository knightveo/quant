# Benchmark protocol

Record GPU model, driver/toolkit version, input shape, warmup count, iteration
count, and correctness status with every result. Setup/allocation time should not
be mixed into steady-state kernel timing unless the benchmark explicitly says so.